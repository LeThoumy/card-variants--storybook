import { styled } from '../../stitches.config';
import type * as Stitches from '../../stitches.config';


const VhBox = styled('main', {
	backgroundColor: '#deeef5',
	h: '100vh',
	display: 'grid',
	placeItems: 'center',
	m: '-1rem'
})

export default VhBox